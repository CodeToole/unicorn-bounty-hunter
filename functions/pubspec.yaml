name: functions
description: Dart backend functions for the UBH Ecosystem
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  firebase_functions: ^0.6.0
  google_generative_ai: ^0.4.7

dev_dependencies:
  build_runner: ^2.10.5
  lints: ^6.0.0
